# Web Buildings

(This is an experimental project.) Design buildings on the web!

## Notes:

- VSCode can't resolve Svelte's `$lib` alias
- VSCode enable linked editing (or AutoRenameTag?)
- `normalize.css` vs `reset.css`
